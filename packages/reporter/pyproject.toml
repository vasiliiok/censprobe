[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "censprobe-reporter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["censprobe-core", "jinja2>=3.1", "weasyprint>=61"]
[tool.hatch.build.targets.wheel]
packages = ["src/reporter"]
